# Server Configuration
server.port=8080

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5433/ticketing_system
spring.datasource.username=postgres
# Modify the default password below to match your local PostgreSQL server's superuser password
spring.datasource.password=${DB_PASSWORD:RidhuxPihu*1619}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.org.springframework.security=INFO
logging.level.com.ticket.system=INFO

# CORS Configuration
# Set this environment variable (APP_CORS_ALLOWED_ORIGINS) on Render to your deployed frontend URL
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,https://nexus-support-psi.vercel.app}

# Spring Mail (SMTP) Configuration (Uncomment and configure for real email dispatch)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ridhamp2005@gmail.com
spring.mail.password=bozk ctja bybp jgyf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


